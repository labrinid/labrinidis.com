---
layout: page
---
<div class="post">
  <article>
    {{ content }}

    {% assign c = site.data.contact %}
    {% assign s = site.data.socials %}
    {% assign obfuscated_email = s.email | replace: '@', ' [at] ' | replace: '.', ' [dot] ' %}

    <div class="row mt-3">
      <div class="col-md-6">

        <h2>Contact</h2>
        <p>
          <strong>Email:</strong> {{ obfuscated_email }}<br>
          <strong>Phone:</strong> {{ c.phone }}<br>
          <strong>Office:</strong> {{ c.building }}, Room {{ c.room }}<br>
          <strong>Office Hours:</strong> {{ c.office_hours }}
        </p>

        <h2>Mailing Address</h2>
        <p>
          {{ c.name }}<br>
          {{ c.department }}<br>
          {{ c.university }}<br>
          {{ c.building }}, Room {{ c.room }}<br>
          {{ c.street }}<br>
          {{ c.city }}, {{ c.state }} {{ c.zip_usps }} <small class="text-muted">(US Postal Service)</small><br>
          {{ c.city }}, {{ c.state }} {{ c.zip_courier }} <small class="text-muted">(courier deliveries)</small><br>
          USA
        </p>

        <h2>Online Profiles</h2>
        <ul>

          {% if s.linkedin_username %}
            <li><strong>LinkedIn:</strong> <a href="https://linkedin.com/in/{{ s.linkedin_username }}" target="_blank">linkedin.com/in/{{ s.linkedin_username }}</a></li>
          {% endif %}
		
          {% if s.medium_username %}
            <li><strong>Medium:</strong> <a href="https://medium.com/@{{ s.medium_username }}" target="_blank">medium.com/@{{ s.medium_username }}</a></li>
          {% endif %}


{% if s.scholar_userid %}
  {% if c.scholar_url %}
    {% assign scholar_link = c.scholar_url %}
  {% else %}
    {% assign scholar_link = 'https://scholar.google.com/citations?user=' | append: s.scholar_userid %}
  {% endif %}
  <li><strong>Google Scholar:</strong> <a href="{{ scholar_link }}" target="_blank">scholar.google.com</a></li>
{% endif %}

{% if s.dblp_url %}
  {% if c.dblp_shorturl %}
    {% assign dblp_link = c.dblp_shorturl %}
  {% else %}
    {% assign dblp_link = s.dblp_url %}
  {% endif %}
  <li><strong>DBLP:</strong> <a href="{{ dblp_link }}" target="_blank">dblp.org</a></li>
{% endif %}

{% if s.orcid_id %}
  {% if c.orcid_url %}
    {% assign orcid_link = c.orcid_url %}
  {% else %}
    {% assign orcid_link = 'https://orcid.org/' | append: s.orcid_id %}
  {% endif %}
  <li><strong>ORCID:</strong> <a href="{{ orcid_link }}" target="_blank">{{ s.orcid_id }}</a></li>
{% endif %}

{% if s.semanticscholar_id %}
  {% if c.semanticscholar_url %}
    {% assign ss_link = c.semanticscholar_url %}
  {% else %}
    {% assign ss_link = 'https://www.semanticscholar.org/author/' | append: s.semanticscholar_id %}
  {% endif %}
  <li><strong>Semantic Scholar:</strong> <a href="{{ ss_link }}" target="_blank">semanticscholar.org</a></li>
{% endif %}
		  		  		  
          {% if s.github_username %}
            <li><strong>GitHub:</strong> <a href="https://github.com/{{ s.github_username }}" target="_blank">github.com/{{ s.github_username }}</a></li>
          {% endif %}
		  

        </ul>

      </div>

      <div class="col-md-6">
        <h2>Location</h2>
        <iframe
          src="{{ c.maps_embed }}"
          width="100%"
          height="300"
          style="border:0;"
          allowfullscreen=""
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade">
        </iframe>
      </div>
    </div>

  </article>
</div>
